language: c++
compiler: gcc

before_install:
    - sudo add-apt-repository ppa:pyglfw/ppa
    - sudo apt-get update
install:
    - sudo apt-get install libgl1-mesa-dev libglew-dev libglfw3-dev libglm-dev
script:
    - "mkdir build && cd build && cmake ../ && make"
